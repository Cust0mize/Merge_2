# Custom mergespecfile for this project
# Disable fallback tools - just fail if Unity can't merge

# Comment out or remove fallback tools to avoid errors
# unity use "%programs%\SomeTool.exe" "%l" "%r" "%b" "%d"
# prefab use "%programs%\SomeTool.exe" "%l" "%r" "%b" "%d"